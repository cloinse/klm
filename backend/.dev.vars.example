# Copy this file to .dev.vars and replace the value with a long random token.
# This token is only for the local administrator and must never be committed.
ADMIN_TOKEN=replace-with-a-long-random-token
